<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Just Tech News</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>


<body>
  <div class="wrapper">
    <!--header section with link to log in-->
    <header>
        <h1>
            <a href="/">Just Tech News</a>
        </h1>
        <nav>
            <!--Show dashboard, logout, and login as appropriate based on whether there is an active session or not-->
            {{#if loggedIn}}
            <a href="/dashboard">dashboard</a>
            <button id="logout" class="btn-no-style">logout</button>
            {{else}}
            <a href="/login">login</a>
            {{/if}}
        </nav>
    </header>
    <!--main section where posts will be populated by handlebars and the database-->
    <main>
      {{{ body }}}
    </main>
    <!--footer section-->
    <footer>
      Thanks for visiting!
    </footer>
  </div>

  <!--Logout Script-->
  <!--load the logout script only if there is an active session-->
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}

</body>

</html>